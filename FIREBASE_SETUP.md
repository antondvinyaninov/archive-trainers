# 🔥 Настройка Firebase для проекта

## Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Добавить проект"** (Add project)
3. Введите название: `archive-trainers` (или любое другое)
4. Отключите Google Analytics (не обязательно для этого проекта)
5. Нажмите **"Создать проект"**

## Шаг 2: Регистрация веб-приложения

1. В консоли Firebase выберите ваш проект
2. Нажмите на иконку **</>** (Web)
3. Введите название приложения: `Archive Trainers Web`
4. **НЕ** ставьте галочку "Firebase Hosting"
5. Нажмите **"Зарегистрировать приложение"**
6. **СКОПИРУЙТЕ** конфигурацию Firebase (firebaseConfig)

## Шаг 3: Настройка Authentication

1. В боковом меню выберите **Build → Authentication**
2. Нажмите **"Начать"** (Get started)
3. Выберите **"Email/Password"**
4. Включите **"Email/Password"** (первый переключатель)
5. Нажмите **"Сохранить"**

## Шаг 4: Настройка Realtime Database

1. В боковом меню выберите **Build → Realtime Database**
2. Нажмите **"Создать базу данных"** (Create Database)
3. Выберите регион: **United States (us-central1)** или ближайший к вам
4. Выберите режим: **"Начать в тестовом режиме"** (Start in test mode)
5. Нажмите **"Включить"** (Enable)

## Шаг 5: Настройка правил безопасности

В Realtime Database перейдите на вкладку **"Правила"** (Rules) и замените содержимое на:

```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin'",
        ".write": "$uid === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin'"
      }
    },
    "results": {
      "$uid": {
        ".read": "$uid === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin'",
        ".write": "$uid === auth.uid"
      }
    },
    "admin": {
      ".read": "root.child('users').child(auth.uid).child('role').val() === 'admin'",
      ".write": "root.child('users').child(auth.uid).child('role').val() === 'admin'"
    }
  }
}
```

Нажмите **"Опубликовать"** (Publish)

## Шаг 6: Вставка конфигурации в код

1. Откройте файл `auth.html`
2. Найдите строку с `firebaseConfig`
3. Замените `YOUR_API_KEY`, `YOUR_PROJECT_ID` и т.д. на ваши значения из шага 2

Пример:
```javascript
const firebaseConfig = {
    apiKey: "AIzaSyC1234567890abcdefghijklmnop",
    authDomain: "archive-trainers.firebaseapp.com",
    databaseURL: "https://archive-trainers-default-rtdb.firebaseio.com",
    projectId: "archive-trainers",
    storageBucket: "archive-trainers.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef1234567890"
};
```

## Шаг 7: Создание первого администратора

1. Зарегистрируйтесь через `auth.html`
2. Перейдите в Firebase Console → Realtime Database
3. Найдите вашего пользователя в разделе `users`
4. Добавьте поле `role` со значением `admin`

Структура должна выглядеть так:
```
users/
  └── UID_пользователя/
      ├── firstName: "Имя"
      ├── lastName: "Фамилия"
      ├── group: "Группа"
      ├── email: "email@example.com"
      ├── role: "admin"  ← Добавьте это поле
      └── createdAt: "2024-01-01T00:00:00.000Z"
```

## Шаг 8: Проверка

1. Откройте `auth.html` в браузере
2. Зарегистрируйте тестового пользователя
3. Проверьте, что данные появились в Realtime Database
4. Попробуйте войти с этими данными

## 🎉 Готово!

Теперь Firebase настроен и готов к использованию!

## 📊 Структура базы данных

```
archive-trainers/
├── users/
│   └── {userId}/
│       ├── firstName: string
│       ├── lastName: string
│       ├── group: string
│       ├── email: string
│       ├── role: "student" | "admin"
│       └── createdAt: timestamp
│
└── results/
    └── {userId}/
        └── {trainerId}/
            ├── attempts: number
            ├── bestScore: number
            ├── bestTime: number
            ├── completed: boolean
            ├── lastAttempt: timestamp
            ├── lastScore: number
            └── examGrade: string (optional)
```

## 🔒 Безопасность

- Студенты видят только свои данные
- Администраторы видят все данные
- Студенты не могут изменять чужие результаты
- Все запросы требуют авторизации

## 💰 Лимиты бесплатного плана

- **Хранилище**: 1 GB
- **Загрузки**: 10 GB/месяц
- **Одновременные подключения**: 100
- **Операции чтения**: 50,000/день
- **Операции записи**: 20,000/день

Этого достаточно для ~100-200 активных студентов.

## 🆘 Проблемы?

### Ошибка "Permission denied"
- Проверьте правила безопасности
- Убедитесь, что пользователь авторизован

### Ошибка "Firebase not defined"
- Проверьте подключение CDN
- Убедитесь, что конфигурация правильная

### Данные не сохраняются
- Откройте консоль браузера (F12)
- Проверьте наличие ошибок
- Проверьте правила безопасности в Firebase Console
